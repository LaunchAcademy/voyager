// we have to separate the component so that storybook's inlining doesn't break things

import React from "react";
import "./typography.css";

export default () => (<h1>H1 | Launch your career in software today.</h1>);
