import blocks, { Meta, Story, Canvas } from "@storybook/addon-docs/blocks"
import Button from "./Button"
import "../../src/assets/scss/main.css"

export const Template = (args) => <Button {...args} />
Template.parameters = {
  backgrounds: { default: "photo" },
}
const defaultArgs = {
  text: "Apply Today",
  to: "#",
  size: "xl",
  placement: "photo",
}

<Meta
  title="Buttons/On Photo BG"
  component={Button}
  parameters={{
    backgrounds: { default: "photo" },
  }}
/>

# Buttons

## Primary Buttons

### Extra-Large (xl)

<Canvas>
  <Story name="Primary XL" args={{ ...defaultArgs, size: "xl" }}>
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

### Large (lg)

<Canvas>
  <Story name="Primary LG" args={{ ...defaultArgs, size: "lg" }}>
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

### Medium (md)

<Canvas>
  <Story name="Primary MD" args={{ ...defaultArgs, size: "md" }}>
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

### Small (sm)

<Canvas>
  <Story name="Primary SM" args={{ ...defaultArgs, size: "sm" }}>
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

## Secondary Buttons

Note: these will show up as "blank" because storybook will not load up the background specified. View them in Canvas if you want to see them in full effect.

### Extra-Large (xl)

<Canvas>
  <Story name="Secondary XL" args={{ ...defaultArgs, size: "xl", secondary: true }}>
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

### Large (lg)

<Canvas>
  <Story name="Secondary LG" args={{ ...defaultArgs, size: "lg", secondary: true }}>
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

### Medium (md)

<Canvas>
  <Story name="Secondary MD" args={{ ...defaultArgs, size: "md", secondary: true }}>
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

### Small (sm)

<Canvas>
  <Story name="Secondary SM" args={{ ...defaultArgs, size: "sm", secondary: true }}>
    {(args) => <Button {...args} />}
  </Story>
</Canvas>
