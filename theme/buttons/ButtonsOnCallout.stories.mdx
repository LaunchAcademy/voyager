import blocks, { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks';
import Button from "./Button"
import "../../src/assets/scss/main.css";

export const Template = (args) => <Button {...args} />
Template.parameters = {
  backgrounds: {default: "callout"},
};
const defaultArgs = {
  text: "Apply Today",
  to: "#",
  size: "xl",
  placement: "callout"
}

<Meta title="Buttons/On Callout" component={Button} parameters={{
  backgrounds: {default: "callout"}}}/>

# Buttons

<ArgsTable of={Button} />

## Primary Buttons

### Extra-Large (xl)

<Canvas parameters={Template.parameters}>
  <Story name="Primary XL" args={{...defaultArgs, size: "xl"}} >
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

### Large (lg)
<Canvas>
  <Story name="Primary LG" args={{...defaultArgs, size: "lg"}}>
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

### Medium (md)

<Canvas>
  <Story name="Primary MD" args={{...defaultArgs, size: "md"}}>
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

### Small (sm)

<Canvas>
  <Story name="Primary SM" args={{...defaultArgs, size: "sm"}}>
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

## Secondary Buttons

### Extra-Large (xl)

<Canvas>
  <Story name="Secondary XL" args={{...defaultArgs, size: "xl", secondary: true}}>
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

### Large (lg)
<Canvas>
  <Story name="Secondary LG" args={{...defaultArgs, size: "lg", secondary: true}}>
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

### Medium (md)

<Canvas>
  <Story name="Secondary MD" args={{...defaultArgs, size: "md", secondary: true}}>
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

### Small (sm)

<Canvas>
  <Story name="Secondary SM" args={{...defaultArgs, size: "sm", secondary: true}}>
    {(args) => <Button {...args} />}
  </Story>
</Canvas>
